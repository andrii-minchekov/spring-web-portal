@author a.minchekov
//If you wish to get DataSource from Container (using META-INF/context.xml) it's necessary to add database driver to container "lib" directory otherwise context will not get started and initialised.
Application use H2 database driver for now, so it's necessary to have h2 database installed on dev environment.

Links:
UML tool ObjectAid UML Explorer
Location: http://www.objectaid.com/update